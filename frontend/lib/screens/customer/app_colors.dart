// Kept for backwards compatibility — all customer screens used to import
// this file as `'app_colors.dart'`. The real definition now lives in
// `lib/utils/app_colors.dart` so both customer AND provider sides share
// the same palette.
export '../../utils/app_colors.dart';
